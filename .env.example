# Optional — without these, app uses local .data/lineage.json (dev)
# or per-instance memory on Vercel.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Comma-separated admin emails (future Auth allowlist)
LIVING_UI_ADMIN_EMAILS=
