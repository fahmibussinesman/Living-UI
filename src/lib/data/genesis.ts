/** @deprecated Prefer `@/lib/data/repo` for async API. Re-exports for tests/local. */
export {
  GENESIS_TOKENS,
  GENESIS_VERSION,
  getHead,
  getVersion,
  getHeadVersion,
  listVersions,
  listMainPath,
  listProposals,
  commitPersonalBranch,
  proposeVersion,
  setHead,
  lockHead,
  castVote,
  toggleFavorite,
  getVisitorVote,
  isFavorited,
  recomputeHead,
  ensureVisitor,
  resetToGenesis,
} from "./store";
